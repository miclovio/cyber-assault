# Ignore all assets by default (only used assets are force-added)
Assets/

# Firebase config (contains API keys)
js/data/firebase-config.js

# Claude config
.claude/

# Node
node_modules/
package.json
package-lock.json

# Large files
*.zip
NUL
